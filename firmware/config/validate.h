#pragma once

#if !defined(PORT_ROWS)
# error PORT_ROWS not configured for target
#endif

#if !defined(DDR_ROWS)
# error DDR_ROWS not configured for target
#endif

#if !defined(PIN_ROWS)
# error PIN_ROWS not configured for target
#endif

#if !defined(PORT_COLS)
# error PORT_COLS not configured for target
#endif

#if !defined(DDR_COLS)
# error DDR_COLS not configured for target
#endif

#if !defined(PIN_COLS)
# error PIN_COLS not configured for target
#endif
